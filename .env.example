# Optional: ohne Keys läuft die App lokal im Browser (Demo).
# Für Vercel + echte DB: beide Werte aus Supabase → Project Settings → API setzen.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_KEY
